import React from "react";
import { View, Text, StyleSheet } from "react-native";

const IndexScreen = () => {
  return (
    <View>
      <Text> pijar larga</Text>
    </View>
  );
};

const estilos = StyleSheet.create({});

export default IndexScreen;
